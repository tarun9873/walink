<IfModule mod_rewrite.c>
  RewriteEngine On

  # don't route requests for existing files/folders
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # route everything else to index.php
  RewriteRule ^ index.php [L]
</IfModule>
